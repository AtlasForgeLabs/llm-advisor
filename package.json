{
  "name": "llm-advisor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check && node scripts/validate-foundation.mjs && node scripts/validate-data.mjs && node scripts/validate-seo.mjs && node scripts/validate-adsense.mjs && node scripts/validate-links.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.0.0",
    "typescript": "^5.6.3"
  }
}
